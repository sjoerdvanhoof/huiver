# Hoisted rather than Bun's default isolated store: Metro resolves react and
# react-native by walking node_modules, and an isolated store can leave the
# Expo app with two copies of them. The web app is indifferent either way.
[install]
linker = "hoisted"
